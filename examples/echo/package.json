{
    "name": "skybox-echo",
    "scripts": {
        "build": "cd ../.. && bash crates/matchbox-cf-worker/examples/build.sh examples/echo examples/echo/EchoListener.bx EchoListener",
        "dev": "npm run build && wrangler dev",
        "deploy": "npm run build && wrangler deploy"
    },
    "devDependencies": {
        "wrangler": "^4.0.0"
    }
}
