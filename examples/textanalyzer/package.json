{
    "name": "skybox-textanalyzer",
    "scripts": {
        "build": "cd ../.. && bash crates/matchbox-cf-worker/examples/build.sh examples/textanalyzer examples/textanalyzer/TextAnalyzerListener.bx TextAnalyzerListener",
        "dev": "npm run build && wrangler dev",
        "deploy": "npm run build && wrangler deploy"
    },
    "devDependencies": {
        "wrangler": "^4.0.0"
    }
}
